{{- if and .Values.queryservice.enabled .Values.queryservice.persistence.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: queryservice-data
  labels:
    {{- include "wikibase.labels" . | nindent 4 }}
  annotations:
  volume.beta.kubernetes.io/storage-class: {{ (.Values.queryservice.persistence.storageClass | quote) | default "default" }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.queryservice.persistence.size | quote }}
    {{- if .Values.queryservice.persistence.storageClass }}
    {{- if (eq "-" .Values.queryservice.persistence.storageClass) }}
      storageClassName: ""
    {{- else }}
      storageClassName: {{ (.Values.queryservice.persistence.storageClass | quote) | default "default" }}
    {{- end }}
    {{- end }}
{{- end -}}
