{{- if .Values.queryservice.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: queryservice-data
  labels:
    {{- include "wikibase.labels" . | nindent 4 }}
  annotations:
  {{- if .Values.queryservice.persistence.storageClass }}
    volume.beta.kubernetes.io/storage-class: {{ .Values.queryservice.storageClass | quote }}
  {{- else }}
    volume.beta.kubernetes.io/storage-class: "default"
  {{- end }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.queryservice.volume.size | quote }}
    {{- if .Values.queryservice.persistence.storageClass }}
    {{- if (eq "-" .Values.queryservice.persistence.storageClass) }}
      storageClassName: ""
    {{- else }}
      storageClassName: {{ .Values.queryservice.persistence.storageClass | quote }}
    {{- end }}
    {{- end }}
{{- end -}}
