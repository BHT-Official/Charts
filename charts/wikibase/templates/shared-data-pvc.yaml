{{- if and .Values.shared.enabled .Values.shared.persistence.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-data
  labels:
    {{- include "wikibase.labels" . | nindent 4 }}
  annotations:
  volume.beta.kubernetes.io/storage-class: {{ (.Values.shared.persistence.storageClass | quote) | default "default" }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.shared.persistence.size | quote }}
    {{- if .Values.shared.persistence.storageClass }}
    {{- if (eq "-" .Values.shared.persistence.storageClass) }}
      storageClassName: ""
    {{- else }}
      storageClassName: {{ (.Values.shared.persistence.storageClass | quote) | default "default" }}
    {{- end }}
    {{- end }}
{{- end -}}
